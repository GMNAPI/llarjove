# OpenAI Configuration
OPENAI_API_KEY=

# Embedding Model
EMBEDDING_MODEL=text-embedding-3-small

# Chat Model
CHAT_MODEL=gpt-4-turbo-preview

# RAG Configuration
MAX_CHUNKS=5
SIMILARITY_THRESHOLD=0.7

# Chroma Vector Database
CHROMA_HOST=localhost
CHROMA_PORT=8000
CHROMA_COLLECTION=legal_docs

# Server Configuration
PORT=3000
HOST=0.0.0.0

# Web Configuration (Next.js) - URL of the RAG API for the frontend
NEXT_PUBLIC_API_URL=http://localhost:3000

# Waitlist (Resend) - set these as Railway env vars for the web service
RESEND_API_KEY=re_xxxxxxxxxxxx
WAITLIST_FROM_EMAIL=LlarJove <hola@llarjove.cat>
WAITLIST_NOTIFY_EMAIL=tu@email.com
